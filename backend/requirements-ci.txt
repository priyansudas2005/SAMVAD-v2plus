# Lightweight CI-only requirements
# Heavy ML packages (torch, transformers, sentence-transformers, faster-whisper)
# are NOT installed in CI — they are mocked in conftest.py instead.

# Web framework
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0

# Utilities
pyyaml>=6.0
loguru>=0.7.0
python-dotenv>=1.0.0

# Testing
pytest>=7.3.0
httpx>=0.24.0
